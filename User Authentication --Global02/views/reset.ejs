<!doctype html>

<head>
    <title>Reset Password</title>
</head>

<body>
    <h1>Reset Password</h1>
    <% if (message.length> 0) { %>
        <div class="alert alert-danger">
            <% if(message.length === 1) { %>
                <strong> <%= message %> </strong>
                <% } else { %>
                    <ul>
                        <% message.forEach(function(err) { %>
                            <li><strong> <%= err %> </strong></li>
                            <% }) %>
                    </ul>
                    <% } %>
        </div>
        <% } %>
            <form action="" method="POST">
                <input type="hidden" name="token" value=token>
                <div>
                    <input name="password" placeholder="Password" required type="password" />
                </div>
                <div>
                    <input name="passwordConfirm" placeholder="Confirm Your Password" required type="password" />
                </div>
                <div>
                    <button class="btn btn-warning btn-lg" type="submit">Login</button>
                </div>
            </form>
            <div> <a href="/login">Login Again or Create a new Account</a></div>
</body>